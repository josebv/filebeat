filebeat:
  prospectors:
    -
      paths:
        - "/var/log/*.log"
        - /var/log/messages
      type: syslog

      #fields:
      #  level: debug
      #  review: 1

      # encoding: utf-8

output:
  logstash:
    enabled: true
    hosts:
      - {{logstash_dns}}:5044
    loadbalance: true
